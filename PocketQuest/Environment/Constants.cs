namespace PocketQuest.Environment
{
    internal class Constants
    {
        /* NOTE: Unless otherwise stated, all units in 
         * meters, kilograms, seconds (MKS):            */
        public const int TILE_WIDTH = 2; 
        public const int TILE_HEIGHT = 1;
        // Path length has [tile] for units:
        public const int MAX_PATH_LENGTH = 32;
        public const int MAX_PATH_HEIGHT = 5;
    }
}
